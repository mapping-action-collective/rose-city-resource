{
  "name": "rose-city-resource",
  "version": "1.0.0",
  "description": "A digital version of the Street Roots Rose City Resource",
  "engines": {
    "node": "14.16.x",
    "npm": "7.9.x"
  },
  "scripts": {
    "postinstall": "npm install --prefix frontend --legacy-peer-deps && npm install --prefix backend --legacy-peer-deps",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "dependencies": {
    "concurrently": "^6.2.0",
    "dotenv": "^10.0.0"
  }
}
